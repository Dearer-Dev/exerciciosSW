//Crie um programa que: Declare um número, calcule o quadrado desse número, mostre o resultado
function opa5(n, potencia) {
    
let calculo = n**potencia

return(`Número escolhido ${n},o calculo da potencia escolhida (${potencia}), dele é ${calculo}`)
}

export {opa5}