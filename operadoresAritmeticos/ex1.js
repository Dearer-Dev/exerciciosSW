//Crie um programa que: Declare duas variáveis com valores numéricos, calcule a soma entre elas, mostre o resultado no console.
function opa1(n1, n2) {

soma = n1 + n2

console.log(`A soma dos números ${n1} e ${2} é igual a ${soma}`)
}

export {opa1}
//Soma