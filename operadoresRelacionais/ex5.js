//Crie um programa que: Declare duas variáveis com valores, verifique se elas são iguais, mostre o resultado

let v1 = 3

let v2 = 2

if(v1 == v2){
    console.log(`A variável 1 é igual a variável 2`)
} else{
    console.log(`As variáveis não são iguais`)
}

//Igualdade (== ou ===); == (igualdade simples)

// O == Ele compara apenas o valor 👉 Ele converte o tipo automaticamente se precisar. === (igualdade estrita) Ele compara: ✔ valor ✔ tipo Não faz conversão automática.

